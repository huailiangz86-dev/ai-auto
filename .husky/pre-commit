#!/usr/bin/env sh
[ -f package.json ] && node -e "require('./package.json').devDependencies['husky']" 2>/dev/null || exit 0
. "$(dirname -- "$0")/_/husky.sh"
echo "🔍 Running pre-commit checks..."
npx lint-staged
echo "✅ Pre-commit checks passed"
